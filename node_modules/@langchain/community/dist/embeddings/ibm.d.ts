import { Embeddings } from "@langchain/core/embeddings";
import { EmbeddingParameters, TextEmbeddingsParams } from "@ibm-cloud/watsonx-ai/dist/watsonx-ai-ml/vml_v1.js";
import { WatsonxAuth, WatsonxParams } from "../types/ibm.js";
export interface WatsonxEmbeddingsParams extends Omit<EmbeddingParameters, "return_options">, Pick<TextEmbeddingsParams, "headers"> {
}
export declare class WatsonxEmbeddings extends Embeddings implements WatsonxEmbeddingsParams, WatsonxParams {
    model: string;
    serviceUrl: string;
    version: string;
    spaceId?: string;
    projectId?: string;
    truncate_input_tokens?: number;
    maxRetries?: number;
    maxConcurrency?: number;
    private service;
    constructor(fields: WatsonxEmbeddingsParams & WatsonxAuth & WatsonxParams);
    scopeId(): {
        projectId: string;
        spaceId?: undefined;
    } | {
        spaceId: string | undefined;
        projectId?: undefined;
    };
    invocationParams(): EmbeddingParameters;
    listModels(): Promise<string[] | undefined>;
    private embedSingleText;
    embedDocuments(documents: string[]): Promise<number[][]>;
    embedQuery(document: string): Promise<number[]>;
}
